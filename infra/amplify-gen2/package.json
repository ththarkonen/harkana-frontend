{
  "name": "@harkana/amplify-gen2",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "cars:sandbox": "cd cars && ampx sandbox --profile default",
    "hcars:sandbox": "cd hcars && ampx sandbox --profile default",
    "raman:sandbox": "cd raman && ampx sandbox --profile default"
  },
  "devDependencies": {
    "@aws-amplify/backend": "^1.24.0",
    "@aws-amplify/backend-cli": "^1.9.0",
    "@types/node": "^26.1.2",
    "aws-cdk-lib": "^2.263.0",
    "constructs": "^10.7.2",
    "typescript": "^7.0.2"
  }
}
